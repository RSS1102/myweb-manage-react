export interface ArticleFormType {
    blogTitle: string,
    blogNavs: { value: number, label: string },
    blogContent: string,
}
export interface blogNavsObj {
    blogTitle: string;
    blogNav: string;
    blogNavId: number;
    blogContent: string;
}